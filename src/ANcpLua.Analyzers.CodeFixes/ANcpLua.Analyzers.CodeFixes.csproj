<Project Sdk="ANcpLua.NET.Sdk">
    <PropertyGroup>
        <TargetFrameworks>net10.0;netstandard2.0</TargetFrameworks>

        <!-- Prevent this from being packaged separately -->
        <PackageId>Dummy.CodeFixes</PackageId>
        <IsPackable>false</IsPackable>

        <!-- Generate XML documentation for DocFX -->
        <GenerateDocumentationFile>true</GenerateDocumentationFile>

        <EnforceExtendedAnalyzerRules>true</EnforceExtendedAnalyzerRules>
        <NeutralLanguage>en</NeutralLanguage>

        <!-- Suppress SDK stricter rules -->
        <!-- RS0030: Banned APIs - analyzer needs access to banned patterns to detect them
             CA1849: Call async methods in async contexts - analyzer constraints
             CA2249: Use Contains - ns2.0 lacks Contains(char, StringComparison), must use IndexOf
             IDE0055: Multi-TFM formatting conflicts (dotnet format creates TFM merge markers)
             MSB3277: System.Threading.Tasks.Extensions version conflict (4.2.0.1 polyfill vs 4.2.1.0
                      from Roslyn.Utilities DLL compiled on newer SDK) - safe for analyzer host loading
             Note: CA1308 handled via file-level pragma in AR0001 (intentional for case conversion) -->
        <NoWarn>$(NoWarn);RS0030;CA1849;CA2249;IDE0055;CS1591;MSB3277</NoWarn>
    </PropertyGroup>

    <ItemGroup>
        <Using Include="Microsoft.CodeAnalysis"/>
        <Using Include="Microsoft.CodeAnalysis.CSharp"/>
        <Using Include="Microsoft.CodeAnalysis.CSharp.Syntax"/>
        <Using Include="Microsoft.CodeAnalysis.CodeFixes"/>
        <Using Include="Microsoft.CodeAnalysis.CodeActions"/>
        <Using Include="Microsoft.CodeAnalysis.CodeRefactorings"/>
        <Using Include="System.Collections.Immutable"/>
        <Using Include="System.Composition"/>
    </ItemGroup>

    <ItemGroup>
        <!-- Source-only polyfills for netstandard2.0 (IsExternalInit, etc.) -->
        <PackageReference Include="ANcpLua.Roslyn.Utilities.Polyfills" PrivateAssets="all"/>
        <PackageReference Include="Microsoft.CodeAnalysis.CSharp.Workspaces" PrivateAssets="all"/>
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\ANcpLua.Analyzers\ANcpLua.Analyzers.csproj"/>
    </ItemGroup>

    <ItemGroup>
        <Compile Update="CodeFixResources.Designer.cs" DesignTime="True" AutoGen="True" DependentUpon="CodeFixResources.resx"/>
        <EmbeddedResource Update="CodeFixResources.resx" Generator="ResXFileCodeGenerator" LastGenOutput="CodeFixResources.Designer.cs"/>
    </ItemGroup>
</Project>
